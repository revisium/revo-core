export const RunErrorText = {
  pipelineSelector: 'Exactly one pipeline selector is required.',
  profileSelector: 'Exactly one profile selector is required.',
  projectIdRequired: 'Project ID is required.',
  runIdAllocationConflict: 'A run ID could not be allocated.',
} as const;
